import {
  BUTTON_HEIGHT,
  ICON_SIZE as DESIGN_ICON_SIZE,
  RADIUS,
  SPACING,
} from "@/constants/design";

export const TOUCH_TARGET = BUTTON_HEIGHT.regular;
export const COMPACT_TOUCH_TARGET = BUTTON_HEIGHT.compact;
export const CONTROL_SPACING = SPACING.sm;
export const CONTROL_PADDING = SPACING.md;
export const PANEL_PADDING = SPACING.md;
export const CONTROL_RADIUS = RADIUS.lg;
export const PILL_RADIUS = RADIUS.pill;
export const ICON_SIZE = DESIGN_ICON_SIZE.lg;
export const HIT_SLOP = {
  top: SPACING.sm,
  bottom: SPACING.sm,
  left: SPACING.sm,
  right: SPACING.sm,
};
